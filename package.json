{
	"name": "bank-shields",
	"version": "1.0.0",
	"description": "Shields.io badges for Hack Club Bank",
	"author": "Gary Tou <gary@garytou.com> (https://garytou.com)",
	"repository": "https://github.com/hackclub/bank-shields",
	"main": "dist/index.js",
	"scripts": {
		"start": "npm run build && ts-node .",
		"build": "rimraf ./dist && tsc",
		"dev": "ts-node-dev --respawn --pretty --transpile-only src/index.ts",
		"lint": "tslint -c tslint.json -p tsconfig.json --fix",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"license": "MIT",
	"prettier": "@garyhtou/prettier-config",
	"devDependencies": {
		"@garyhtou/prettier-config": "^1.0.1",
		"@types/cors": "^2.8.12",
		"@types/express": "^4.17.13",
		"ts-node-dev": "^1.1.8",
		"tslint": "^6.1.3"
	},
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^10.0.0",
		"express": "^4.17.1",
		"helmet": "^4.6.0",
		"rimraf": "^3.0.2",
		"ts-node": "^10.2.1",
		"typescript": "^4.4.2"
	}
}
