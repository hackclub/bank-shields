// Utils are used as a wrapper for external packages and libraries. For example,
// Slack's Bolt.js, Twilio, GitHub Octokit, or Passport.js.

// import the package here

// Then initialize and set the configuration (such as adding API Keys)

const someExternalPackage = {};

// Export the initialized package
export default someExternalPackage;
